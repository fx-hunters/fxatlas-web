/**
 * 루트 레이아웃·라우팅 진입 컴포넌트.
 * 화면은 src/screens/ 아래(home·xray·forecast·route·mypage)로 라우팅한다.
 * TODO(미확정): 라우터 라이브러리 선정 (CLAUDE.md 9장).
 */
export function App() {
  return (
    <main>
      <h1>FxAtlas</h1>
    </main>
  );
}
